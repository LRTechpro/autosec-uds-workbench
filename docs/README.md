# docs/

Drop application screenshots here and reference them from the main README:

- `workbench_fail_trace.png` - the security-fail trace with FAIL verdicts
  (the money shot: shows the tool catching something)
- `workbench_pass_trace.png` - a clean PASS run

Recommended capture: security-fail or flash-sequence-fail trace loaded,
validation run, a FAIL row selected so the decoded detail pane is populated.
